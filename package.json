{
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  }
}
